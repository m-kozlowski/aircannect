[env:xiao-esp32s3-plus-sdmmc4-ota]
extends = env:xiao-esp32s3-plus-sdmmc4
upload_protocol = espota

; Put secrets and per-device defaults in gitignored platformio-private.ini.
;
; [env:xiao-esp32s3-plus-sdmmc4-ota]
; upload_port = aircannect-host
; upload_flags =
;     --auth=your-local-ota-password
;
