Place upstream libsmb2 patches here as `*.patch`.

`generate_libsmb2.py` applies them, in sorted order, to the generated copy under
`.pio/generated-libs/libsmb2`. The `third_party/libsmb2` submodule stays
unmodified.
